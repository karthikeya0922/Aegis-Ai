{
  "name": "@aegis/mock-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone mock of Person 1's Aegis security engine — implements the /internal/* contract with realistic fake data so the frontend is never blocked.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch server.ts",
    "start": "tsx server.ts",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^20",
    "tsx": "^4.19.2",
    "typescript": "^5"
  }
}
