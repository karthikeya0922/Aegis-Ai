# Aegis Inspector -- ML extras.
#
# Split out from requirements.txt on purpose: the service must boot and serve
# the full contract before any of this is installed, so Person 2 is never
# blocked on a multi-gigabyte download.
#
# Phase 2/3  -> presidio + spacy        (PII, India recognizers)
# Phase 9    -> sentence-transformers   (semantic cache vectors)
# Phase 10   -> cross-encoder NLI       (grounded verification)
#
# After installing, fetch the spaCy model:
#     python -m spacy download en_core_web_lg

-r requirements.txt

presidio-analyzer==2.2.357
presidio-anonymizer==2.2.357
spacy==3.8.3

sentence-transformers==3.3.1
torch==2.5.1
scikit-learn==1.6.0
