# Aegis Inspector -- base runtime.
# Installs and boots without any ML model download. Phases 0, 1, 4, 5, 6, 8.
# For the model-backed phases (2, 3, 9, 10, 11) also install requirements-ml.txt.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0
python-dotenv==1.0.1
PyYAML==6.0.2
httpx==0.28.1

# Persistence (Phase 8)
SQLAlchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# Numerics (entropy, fairness metrics)
numpy==2.2.1

# Testing
pytest==8.3.4
pytest-asyncio==0.25.0
